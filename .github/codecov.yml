ignore:
  - "tests/**/*"

coverage:
  range: 60..90
  precision: 1
  status:
    project:
      default:
        threshold: 0.5%
    patch:
      default:
        threshold: 1%

comment:
  layout: "reach, diff, flags, files"
  require_changes: true
  show_carryforward_flags: true
